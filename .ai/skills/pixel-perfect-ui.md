# Pixel-Perfect UI

Use the supplied reference as the visual source of truth while implementing independently. Match the target viewport first, then tune shell width, typography, spacing, image crops, borders, shadows, hover states, modal sizing, and transitions in that order. Use screenshots at identical viewport dimensions for comparison. Prefer measured CSS values and existing components over generic framework defaults.
